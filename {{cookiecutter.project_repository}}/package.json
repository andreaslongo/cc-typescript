{
  "jest": {
    "clearMocks": true,
    "coverageDirectory": "coverage",
    "preset": "ts-jest",
    "roots": [
      "<rootDir>/src/",
      "<rootDir>/test/"
    ],
    "testEnvironment": "node"
  },
  "scripts": {
    "coverage": "jest --coverage",
    "test": "jest"
  }
}
